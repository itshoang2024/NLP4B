# ══════════════════════════════════════════════════════════════════════════════
# requirements.txt — Embedding Service (CPU-only)
# ══════════════════════════════════════════════════════════════════════════════
# Install: pip install --no-cache-dir -r requirements.txt
# Note: torch CPU wheel is pulled via --extra-index-url in Dockerfile

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0

# Embedding models
sentence-transformers>=3.3.0
fastembed>=0.4.0
transformers>=4.45.0

# PyTorch CPU — installed separately in Dockerfile via extra-index-url
# torch>=2.5.0

# Utilities
numpy>=1.26.0
